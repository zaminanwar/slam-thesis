# slam_toolbox configuration - AUTO-GENERATED FOR TUNING
# DO NOT EDIT - Generated by Optuna hyperparameter tuning

slam_toolbox:
  ros__parameters:
    # === Frame Configuration ===
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan

    # === Timing ===
    use_sim_time: true
    restamp_tf: true

    # === Mode ===
    mode: mapping

    # === Solver Parameters ===
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # === Map Parameters ===
    resolution: 0.05
    map_update_interval: 5.0
    max_laser_range: 10.0
    minimum_time_interval: 0.5
    transform_timeout: 2.0
    tf_buffer_duration: 60.0

    # === Scan Matching (TUNED) ===
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.2096
    minimum_travel_heading: 0.3248

    # === Loop Closure (TUNED) ===
    do_loop_closing: true
    loop_match_minimum_chain_size: 3
    loop_match_maximum_variance_coarse: 1.9810
    loop_match_minimum_response_coarse: 0.2139
    loop_match_minimum_response_fine: 0.4823

    # === Correlation Search (TUNED) ===
    correlation_search_space_dimension: 0.5396
    correlation_search_space_resolution: 0.0127
    correlation_search_space_smear_deviation: 0.1

    # === Loop Search (TUNED) ===
    loop_search_space_dimension: 11.7251
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03
    loop_search_maximum_distance: 5.2336

    # === Scan Buffer ===
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0

    # === Map Thresholds (TUNED) ===
    link_match_minimum_response_fine: 0.1512
    link_scan_maximum_distance: 1.5

    # === Odometry (TUNED) ===
    distance_variance_penalty: 0.6301
    angle_variance_penalty: 2.3978
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349

    # === Stack Size ===
    stack_size_to_use: 40000000

    # === Interactive Mode ===
    enable_interactive_mode: false
